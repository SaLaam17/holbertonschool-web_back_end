#!/usr/bin/env python3
"""
Module for a type-annotated function called "floor"
which takes a float "n" as argument and returns the floor of the float.
"""

import math


def floor(n: float) -> int:
    """
    Type-annotated function called "floor"
    which takes a float "n" as argument and returns the floor of the float.
    """
    return math.floor(n)
