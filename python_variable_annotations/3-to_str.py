#!/usr/bin/env python3
"""
Module for a type-annotated function called "to_str"
that takes a float "n" as argument
and returns the string representation of the float.
"""


def to_str(n: float) -> str:
    """
    Type-annotated function called "to_str"
    that takes a float "n" as argument
    and returns the string representation of the float.
    """
    return str(n)
